# 공식 MCP Python SDK (FastMCP 포함).
# 설치: <가상환경 파이썬> -m pip install -r requirements.txt
#
# 2.x 에서 FastMCP 가 mcp.server.fastmcp -> mcp.server.mcpserver 로 이동했다.
# server.py 는 `from mcp.server.fastmcp import FastMCP` 를 쓰므로 1.x 로 고정한다.
# 고정하지 않으면 최신 2.x 가 설치되어
#   ModuleNotFoundError: No module named 'mcp.server.fastmcp'
# 가 발생한다.
mcp[cli]>=1.2.0,<2
